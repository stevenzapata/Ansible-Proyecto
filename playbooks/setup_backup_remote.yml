---
- name: Configurar backups remotos
  hosts: "{{ target_hosts | default('all') }}"
  become: true

  roles:
    - role: ssh_keygen       # genera el par de claves si no existe
    - role: backup_remote    # configura el cron de rsync usando esa clave
